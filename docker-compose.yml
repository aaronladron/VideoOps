version: '3.8'

services:
  web:
    build:
      context: ./web
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    container_name: videoops-web
    restart: unless-stopped
